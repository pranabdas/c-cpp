---
title: Add two integers
---
In the following program, we will ask the user to enter two integer inputs. The
programs will calculate and print the sum of two numbers.

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';

<Tabs groupId="language">

<TabItem value="C">

import add_two_integers_c from '!!raw-loader!/src/c/basics/01-add-two-integers.c';

<CodeBlock language="c" title="src/c/basics/01-add-two-integers.c" showLineNumbers>{add_two_integers_c}</CodeBlock>

</TabItem>

<TabItem value="C++">

import add_two_integers_cpp from '!!raw-loader!/src/cpp/basics/01-add-two-integers.cpp';

<CodeBlock language="cpp" title="src/cpp/basics/01-add-two-integers.cpp" showLineNumbers>{add_two_integers_cpp}</CodeBlock>

</TabItem>
</Tabs>

:::danger

```cpp
int a = 123456789;
long long b = a * a; // this is wrong, a * a will result in int type
long long c = (long long) a * a; // or change the type of a to long long
```

:::


:::caution

It is risky to compare floating point numbers with `==` due to precision errors.
Better approach is to check the difference is less than a small number:
```cpp
if (abs(a - b) < 1e-9)
{
    // a and b are equal
}
```

:::
